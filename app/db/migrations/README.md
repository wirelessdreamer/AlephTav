# Migrations

SQLite derived indexes are intentionally rebuildable from canonical JSON plus licensed upstream manifests.

If irreversible schema migrations are introduced later, record them in this directory.
