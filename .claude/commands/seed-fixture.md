---
description: Fast bootstrap with the small fixture corpus (no API/UI start).
allowed-tools: Bash(./setup.sh:*)
---

Run the platform setup script in fixture mode without starting servers.
Useful before running the integration or unit tests.

!`./setup.sh --fixture --skip-start`
